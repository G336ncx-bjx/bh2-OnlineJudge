# OJ 系统梯度压力测试报告

> 测试时间：2026-09-03 09:35 ~ 09:55
> 测试方式：从低到高逐档递增并发，每档统计成功率 / 延迟分位 / 429 限流 / 错误分布
> 压测脚本：`stress_test.py`（requests + ThreadPoolExecutor，登录态真实请求）

---

## 一、测试结论速览

| 场景 | 稳定区间 | 拐点 | 拐点现象 | 结论 |
|---|---|---|---|---|
| 读接口 | 并发 ≤ 200 | 并发 300 | 成功率 100% → 12.8%，大量连接失败 | 单进程事件循环饱和 |
| 评测提交 | 并发 ≤ 5 | 并发 ≥ 10 | 大量 429 限流 | 限流机制正常工作（符合题目要求） |
| AI 命题 | 并发 ≤ 3 | 并发 4 | 1/9 失败 + 2/9 永久卡 running | 并发下 LLM 调用健壮性不足 |

---

## 二、场景 1：读接口并发

**方法**：登录态轮流请求 4 个读接口（题目列表 / 题目详情 / 语言列表 / 提交列表）。

| 并发 | 请求数 | 成功率 | P50 | P95 | P99 | 错误 |
|---|---|---|---|---|---|---|
| 10 | 100 | 100% | 0.079s | 0.107s | 0.185s | 0 |
| 50 | 500 | 100% | 0.318s | 1.617s | 2.947s | 0 |
| 100 | 1000 | 100% | 0.355s | 5.060s | 8.295s | 0 |
| 200 | 2000 | 100% | 0.360s | 12.260s | 16.314s | 0 |
| **300** | 3000 | **12.8%** | — | — | — | 2574 连接失败 + 42 个 502 |
| **500** | 5000 | **12.6%** | — | — | — | 4343 连接失败 + 26 个 502 |

**分析**：
- 并发 ≤ 200 时**零失败**，但 P95 延迟随并发线性恶化（0.1s → 12.3s），说明请求在事件循环里排队，系统吞吐已接近极限但未崩。
- **并发 300 是拐点**：成功率骤降到 12.8%，绝大多数是客户端「连接失败」（`-1`，Windows socket 连接队列满 / 事件循环饱和），少量 502（uvicorn 上游处理超时）。
- 根因：单进程 uvicorn + 同步 JSON 文件读写（每次请求重读磁盘文件），高并发下事件循环被文件 I/O 阻塞，无法及时接受新连接。

**建议**：这是单进程架构的天然瓶颈。若要提升，可增加 uvicorn worker 数、或给存储层加内存缓存减少磁盘 I/O。但对课程作业场景，200 并发零失败已足够。

---

## 三、场景 2：评测提交吞吐

**方法**：注册 30 个压测账号绕开单用户限流，并发提交 AC 代码（P1002 阶乘求和）。

| 并发 | 请求数 | 成功率 | 200 | 429 限流 | P95 |
|---|---|---|---|---|---|
| 5 | 50 | 100% | 50 | 0 | 0.026s |
| 10 | 100 | 45% | 45 | 55 | 0.051s |
| 20 | 200 | 3.5% | 7 | 193 | 0.047s |
| 30 | 300 | 3.0% | 9 | 291 | 0.062s |
| 50 | 500 | 3.4% | 17 | 483 | 0.351s |

**分析**：
- 429 大量出现**不是系统崩溃，而是限流正常工作**。题目规定「1 分钟内提交超过 3 次返回 429」（`api.md` 第 26 行）。
- 30 个账号 × 3 次/分钟 = 理论放行上限约 90 次/分钟，并发 ≥ 10 时请求量超过该配额，多出部分被 429 正确拒绝。
- 关键健康信号：**放行的提交全部评测成功**（174 个提交最终 status 全部 `success`，score=30/3 测试点全 AC），评测队列单 worker 串行消费及时，**无积压、无堆积**。

**结论**：限流 + 单 worker 评测队列配合良好，系统在限流保护下吞吐稳定。评测引擎本身不是瓶颈，限流才是（符合设计意图）。

---

## 四、场景 3：AI 命题并发

**方法**：并发提交「数组前缀和」命题需求，等待任务跑完（真实调用 DeepSeek）。

### 4.1 提交接口性能

| 并发 | 提交成功率 | P50 | P95 |
|---|---|---|---|
| 2 | 100% | 0.403s | 0.403s |
| 3 | 100% | 0.282s | 0.555s |
| 4 | 100% | 0.665s | 0.994s |

提交接口在并发下 100% 成功，无 429（AI 命题接口未限流）。

### 4.2 任务最终结果（9 个任务）

| 状态 | 数量 | 说明 |
|---|---|---|
| ✅ completed | 6 | 成功出题（「前缀和查询」「区间求和」等） |
| ❌ failed | 1 | 测试点生成阶段 JSON 解析失败 |
| ⏳ running（卡死） | 2 | 停在「生成测试点」超过 10 分钟未结束 |

**费用**：本次压测 9 个任务总费用 **¥1.57**（DeepSeek，CNY 计价）。

### 4.3 暴露的两个并发缺陷

**缺陷 1：并发下测试点生成偶发失败（1/9）**

- 失败任务 `61ea9350`：第二阶段输出 18400 token，JSON 解析失败。
- 根因：并发时 DeepSeek 对部分请求返回更长的测试点输出，逼近单次 `max_tokens=16384` 上限被截断，导致测试点 JSON 不完整。
- 单任务串行跑时稳定（历史多次单任务均成功），并发下偶发。

**缺陷 2：LLM 请求挂起无兜底（2/9）**

- 两个任务 `c34de42f`、`c40ed131` 停在「生成测试点」**超过 10 分钟**，既不超时报错也不完成。
- 根因：`call_llm` 设置了 `httpx.AsyncClient(timeout=120)`，但 DeepSeek 在并发下部分请求响应挂起（疑似流式响应读到一半停住），httpx 的读超时会在收到数据块时被重置，导致**超时机制失效**，协程永久 await。
- 影响：这两个任务永远占用 running 状态，且占用的协程一直挂起。

---

## 五、总体评估

| 维度 | 评价 |
|---|---|
| 读接口抗压 | 良好（200 并发零失败），300+ 是单进程架构极限 |
| 限流机制 | 正确可靠（并发下精确触发 429） |
| 评测吞吐 | 稳定无积压，限流是设计内的瓶颈 |
| AI 命题并发 | 提交层稳定，但并发出题有健壮性问题（截断 + 挂起） |

**核心结论**：基础模块（题目 / 评测 / 用户 / 日志 / 前端）在压力下表现稳健，限流和评测队列设计合理。**唯一值得修复的是 AI 命题在并发下的两个健壮性缺陷**——尤其是「LLM 请求挂起无兜底」，因为它在真实多用户场景下会导致任务永久卡死。

---

## 六、建议修复项（待确认）

1. **【推荐】给 LLM 调用加硬超时兜底**：在 `call_llm` 外层用 `asyncio.wait_for` 包裹，强制整体截止时间（如 120s），超时抛异常 → 任务置 failed，避免永久卡 running。
2. **【可选】测试点生成阶段重试**：阶段二解析失败时重试一次（或调低该阶段 `max_tokens`），降低并发下的偶发失败率。
3. **【可选】限制 AI 命题并发数**：用 `asyncio.Semaphore` 限制同时进行的 LLM 调用数（如 2-3 个），从根上避免并发过载。

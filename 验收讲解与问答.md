# OJ 系统从零详解 —— 跟着代码理解每一处实现

> 本文按"从零开始"的顺序写：先讲懂几个必须知道的概念，再按模块把代码的每一处关键实现拆开讲。
> 目标：验收时无论助教问哪块，你都能讲出"它是什么、怎么写的、为什么这样写"。

---

# 第一部分：前置概念（不懂这些就看不懂代码）

## 1.1 什么是"后端"和"前端"

- **后端（backend）**：一个常驻运行的程序，监听一个端口（我们用的是 8000），别人通过 HTTP 请求问它要数据/让它干活。我们的后端用 **FastAPI**（Python 的一个 Web 框架）写的。
- **前端（frontend）**：你在浏览器里看到的页面。我们的前端用 **Streamlit**（Python 的一个页面框架）写的，它自己不存数据，一切数据都通过 HTTP 请求问后端要。

两者的关系：
```
浏览器 (Streamlit 页面)  ---HTTP 请求--->  FastAPI 后端 (127.0.0.1:8000)
                              <--JSON 响应---
```
"127.0.0.1" 就是"本机自己"，验收时两个程序都在你电脑上跑。

## 1.2 什么是异步（async）——这门课的核心考点

**同步（sync）**：像排队打饭，你点完菜，站窗口前等师傅做完给你，期间你啥也干不了。
**异步（async）**：像去餐厅点单，你点完拿个号牌坐下玩手机，菜好了叫你。等菜的这段时间，服务员可以去服务别人。

在 Python 里：
```python
# 同步：这一行要等 10 秒才返回，整个程序卡住 10 秒
result = do_slow_thing()

# 异步：await 表示"我先去干别的，这个好了叫我"
result = await do_slow_thing()
```

**这门课的硬性要求**："所有后端 API 必须用 async def"。所以你看我们 `app/routers/*.py` 里所有接口函数都是 `async def`。

**为什么评测系统特别需要异步**：评测一个提交要等用户程序跑完（可能几秒）；AI 命题要等大模型回复（可能几十秒）。如果是同步的，等待期间整个后端不能响应任何其他人的请求。异步让这些"等待"不阻塞别人。

**协程（coroutine）**：`async def` 定义的函数就是协程。**事件循环（event loop）**：后台调度器，管理所有协程，谁在等、谁可以继续跑，由它统一调度。FastAPI 帮你把事件循环跑起来了，你只管写 `async def` + `await`。

## 1.3 什么是子进程（subprocess）——评测的基础

我们的后端是 Python 程序。要评测用户提交的 C++/Python 代码，不能让用户的代码直接在我们的 Python 进程里跑（危险，而且没法限资源），所以要**新开一个独立的进程**去跑用户代码：

```python
proc = await asyncio.create_subprocess_exec(*cmd, ...)
```

这就是"子进程"。好处：
1. 用户程序崩溃（RE）不会拖垮评测系统；
2. 可以单独监控它的时间和内存；
3. 可以随时把它"杀掉"（TLE/MLE 时）。

我们用 **psutil** 这个库来查子进程的内存占用、杀子进程。

## 1.4 什么是 JSON

一种文本数据格式，长得像 Python 的字典/列表。我们的题目、用户、提交记录全部存成 `.json` 文件。选它的原因：可读、可手改、Python 自带支持（`json.loads` 读、`json.dumps` 写）。

---

# 第二部分：系统整体架构

```
┌─────────────── 前端 frontend/app.py (Streamlit, 1991 行) ───────────────┐
│  登录页 / 题目页 / 提交页 / 用户页 / 管理页 / AI 命题页                  │
│  一切数据都通过 HTTP 请求后端，不直接碰数据文件                          │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │ HTTP (127.0.0.1:8000)
┌───────────────────────────────▼─────────────────────────────────────────┐
│ 后端 app/main.py (FastAPI 入口)                                         │
│   ├─ app/routers/   六组 API 路由（HTTP 请求的"门面"）                  │
│   │    problems.py  submissions.py  users.py  languages.py  logs.py  ai.py │
│   ├─ app/deps.py    登录态校验、权限判断、提交频率限制                    │
│   ├─ app/judge/     评测核心                                            │
│   │    queue.py     评测队列（后台 worker 串行消费）                     │
│   │    engine.py    评测流程（编译→运行→比对→判定）                     │
│   │    runner.py    进程执行器（时间限制、内存限制在这里实现）            │
│   ├─ app/ai/        AI 命题                                            │
│   │    llm.py       调用大模型、费用计算、JSON 解析兜底                   │
│   │    engine.py    命题流水线（六阶段）                                 │
│   └─ app/storage.py 数据读写（全部 JSON 文件的唯一出入口）               │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │ 读写 JSON
                    data/ （题目、用户、提交、审计日志…）
```

**记住这个分层**，被问到"XX 功能在哪实现"，先定位它在哪一层，再讲具体函数。

---

# 第三部分：逐模块详解

## 3.1 config.py —— 全局配置

很短，全是常量。关键几行：

```python
DEFAULT_TIME_LIMIT = 3.0      # 题目没写时限时的默认值：3 秒
DEFAULT_MEMORY_LIMIT = 128    # 默认内存：128 MB
COMPILE_TIME_LIMIT = 30.0     # 编译单独给 30 秒（比运行时限大很多）
TESTCASE_SCORE = 10           # 一个测试点 10 分
AI_LLM_TIMEOUT = 600.0        # 大模型调用硬超时 10 分钟
AI_LLM_MAX_RETRIES = 4        # 网络抖动自动重试 4 次
```

**为什么编译单独给 30 秒**：Windows 上 g++ 第一次运行有"冷启动"（要加载编译器的各种组件），可能慢。如果拿 3 秒的运行时限去卡编译，正常代码会被误判 CE。所以编译用独立的大时限。

## 3.2 storage.py —— 存储层（所有数据文件的唯一出入口）

所有模块读写数据都经过这里，两个核心函数：

### _read_json：读文件（带缓存）

```python
def _read_json(path: str, default: Any) -> Any:
    if not os.path.exists(path):
        return default                     # 文件不存在返回默认值
    st = os.stat(path)                     # 拿文件的状态（修改时间、大小）
    key = (st.st_mtime_ns, st.st_size)     # 用「修改时间+大小」当缓存钥匙
    cached = _read_cache.get(path)
    if cached and cached[0] == key[0] and cached[1] == key[1]:
        return cached[2]                   # 文件没变过 → 直接返回缓存，不读盘
    with open(path, "r", encoding="utf-8") as f:
        data = json.load(f)                # 变了才真正读盘+解析
    _read_cache[path] = (key[0], key[1], data)
    return data
```

**为什么加缓存**（这是验收可能问的性能点）：评测列表接口要读几百个提交文件，每个都 `open + json.load` 要花 1.6 秒。缓存逻辑是"文件没变就不重新读盘"。实测优化到 0.04 秒，约 40 倍。

**缓存不会读到旧数据吗**？不会——所有写操作都用 `os.replace`（见下），replace 一定会改文件的修改时间，下次读时 key 对不上，自动重新读盘。

### _write_json：写文件（原子替换）

```python
def _write_json(path: str, obj: Any) -> None:
    tmp = path + ".tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        json.dump(obj, f, ensure_ascii=False, indent=2)   # 先写到 .tmp
    os.replace(tmp, path)      # 再把 .tmp 原子地改名成正式文件
```

**为什么先写 .tmp 再 replace**：如果直接写正式文件，写到一半程序崩了/断电，文件就损坏了（半个 JSON）。先写临时文件、写完整了再"改名替换"——操作系统保证改名是原子操作（要么旧文件、要么新文件，不存在中间态）。

## 3.3 deps.py —— 登录态、权限、频率限制

### get_current_user：从 session 读当前用户

```python
def get_current_user(request: Request):
    user_id = request.session.get("user_id")   # session 是登录时写入的
    if user_id is None:
        return None                            # 没登录
    user = storage.get_users().get(user_id)
    if user is None:
        return None
    cookie_sv = request.session.get("sv")      # 会话版本号
    stored_sv = user.get("session_version", 0)
    if cookie_sv != stored_sv:
        return None                            # 版本对不上 = 会话已失效
    return user
```

**会话版本号是干什么的**：解决"用户登出后，旧的 cookie 还能不能继续用"的问题。登出时把用户数据里的 `session_version` +1，那么之前签发的所有 cookie（里面存着旧的版本号）全部对不上 → 全部失效。这是一个常见的安全细节。

### check_rate_limit：提交频率限制

```python
def check_rate_limit(user_id: str, problem_id: str) -> bool:
    now = time.time()
    key = (user_id, problem_id)                # 按「用户+题目」分别计数
    records = _rate_records.setdefault(key, [])
    records[:] = [t for t in records if now - t < 60]   # 丢掉 1 分钟前的
    if len(records) >= 3:
        return False                           # 同题 1 分钟内第 4 次 → 拒绝
    records.append(now)
    return True
```

**口径**："单人单题" 1 分钟最多 3 次。注意 key 是 `(user_id, problem_id)`——同一用户换一道题不受影响（这是验收时按老师反馈改过的口径，原来是不分题的全局限制）。

## 3.4 judge/runner.py —— 进程执行器（时间、内存限制的实现在这里！）

这是"时间限制如何检查、内存限制如何检查"的答案所在，**最可能被点名**。

### 先看数据结构 RunResult

```python
@dataclass
class RunResult:
    returncode: int      # 退出码：0=正常，非0=出错
    stdout: str          # 程序的标准输出
    stderr: str          # 程序的标准错误输出
    time_cost: float     # 耗时（秒）
    memory_cost: float   # 峰值内存（MB）
    timed_out: bool      # 是否超时
    memory_exceeded: bool  # 是否内存超限
```

### run_command：执行一条命令并施加资源限制

```python
async def run_command(cmd, stdin_data="", time_limit=3.0, memory_limit=128.0, ...):
    # 1) 拉起子进程，三根"管道"都接上
    proc = await asyncio.create_subprocess_exec(
        *cmd,
        stdin=asyncio.subprocess.PIPE,    # 我们能往里写（喂输入）
        stdout=asyncio.subprocess.PIPE,   # 我们能读它的输出
        stderr=asyncio.subprocess.PIPE,   # 我们能读它的报错
        cwd=cwd,
    )

    # 2) 同时启动一个"内存监控协程"，在后台盯着内存
    flag = {"mle": False, "peak": 0}
    monitor = asyncio.ensure_future(_monitor_memory(proc, memory_limit, flag))

    # 3) 喂输入、收输出，但用 wait_for 卡一个时间上限
    try:
        stdout_b, stderr_b = await asyncio.wait_for(
            proc.communicate(stdin_data.encode("utf-8")),
            timeout=time_limit,          # ← 时间限制就这一行
        )
        timed_out = False
    except asyncio.TimeoutError:
        timed_out = True                 # 超时！
        stdout_b, stderr_b = b"", b""

    # 4) 收尾：超时就把进程杀掉；确保进程一定被回收
    finally:
        monitor.cancel()
        if timed_out:
            _kill(proc)
    ...
```

**时间限制的原理**（三句话讲清）：
1. `asyncio.wait_for(..., timeout=time_limit)` —— 用**事件循环的时钟**给"等子进程跑完"这件事设一个截止时间；
2. 到时间没跑完 → 抛 `TimeoutError` → 标记 `timed_out=True`；
3. 然后 `_kill(proc)` **强制杀掉子进程**（连同它的子进程）。

**诚实口径**：这是"墙钟时间"（真实流逝的秒数），不是 Linux OJ 常用的"CPU 时间"。因为 Windows 上 Python 没有 `resource` 模块拿不到 CPU 时间。对正常代码没影响（题目时限都留了余量），死循环会被正确杀掉。

### _monitor_memory：内存监控协程

```python
async def _monitor_memory(proc, limit_mb, flag):
    p = psutil.Process(proc.pid)
    while True:
        if proc.returncode is not None:
            break                        # 进程已退出，不监控了
        rss = p.memory_info().rss        # 读进程当前占用内存（字节）
        peak = rss
        for child in p.children(recursive=True):   # 加上所有子进程
            peak += child.memory_info().rss
        if peak > flag["peak"]:
            flag["peak"] = peak          # 记录峰值
        if peak > limit_mb * 1024 * 1024:  # 超限（MB 换算成字节）
            flag["mle"] = True
            _kill(proc)                  # 立即杀掉
            break
        await asyncio.sleep(0.05)        # 每 50 毫秒采样一次
```

**内存限制的原理**（三句话讲清）：
1. 一个后台协程**每 50ms 采样一次**进程的内存占用；
2. 峰值 = 主进程 + 递归所有子进程（防止用户程序再开子进程逃过监控）；
3. 一旦超过 `memory_limit`（MB）→ 标记 MLE 并**立即杀进程**。

**为什么必须"边跑边轮询采样"，而不是"跑完查一次"**：进程退出后它的内存信息就没了（`psutil` 读不到），所以只能在运行期间持续采样、记录峰值。你看到的"内存"就是采样到的最大值。

### _kill：杀掉进程（连同子进程）

```python
def _kill(proc):
    parent = psutil.Process(proc.pid)
    for child in parent.children(recursive=True):
        child.kill()      # 先杀所有子进程
    parent.kill()         # 再杀主进程
```

防止"杀主进程漏子进程，子进程还在偷偷跑占用资源"。

## 3.5 judge/engine.py —— 评测流程（编译→运行→比对→判定）

### judge_submission：评测一条提交的完整流程

```python
async def judge_submission(submission: dict) -> dict:
    # 取题目、取语言配置
    problem = storage.get_problem(problem_id)
    lang = storage.get_languages().get(language_name)

    # 资源限制三级兜底：题目配置 > 语言默认 > 全局默认
    time_limit = problem.get("time_limit") or lang.get("time_limit") or 3.0
    memory_limit = problem.get("memory_limit") or lang.get("memory_limit") or 128

    # 为这次评测建一个独立临时目录（uuid 命名，互不干扰）
    tmp_dir = _prepare_temp_dir()
    src_path = tmp_dir / "main.cpp"（按语言的文件扩展名）
    # 把用户代码写进 main.cpp
    # 1) 编译（用独立的大时限）
    compile_res = await compile_source(...)
    # 编译失败 → 所有测试点 CE，0 分，结束
    # 2) 逐个测试点运行：
    for tc in testcases:
        run_res = await run_source(..., tc["input"], time_limit, memory_limit)
        result, actual = _classify(run_res, tc["output"])
        # 记录这个测试点的 result/time/memory
    # 3) 算分：每个 AC 的测试点 10 分
    submission["score"] = ac_count * config.TESTCASE_SCORE
    # finally：删除临时目录（不留垃圾）
```

**几个值得讲的点**：

- **每个测试点都重新运行一次程序**：OJ 的常规做法。同一个程序对每个测试点的输入跑一遍，每次独立计时、独立判内存。
- **临时目录用 `uuid4().hex` 命名**：同时有多个评测时（理论上），各自的源码文件不会互相覆盖。
- **编译失败的处理**：不逐点运行了，直接所有测试点标记 CE、0 分。
- **异常兜底**：`except Exception → status = "error"`，评测器自己绝不能因为用户代码的奇形怪状而崩溃。

### _classify：判定顺序（顺序是故意设计的）

```python
def _classify(run_res, expected):
    if run_res.timed_out:          return "TLE", ...   # 1. 先查超时
    if run_res.memory_exceeded:    return "MLE", ...   # 2. 再查内存
    if run_res.returncode != 0:    return "RE", ...    # 3. 再查崩溃
    if run_res.error:              return "UNK", ...   # 4. 执行器异常
    if compare_output(stdout, expected): return "AC", ...
    return "WA", ...                                   # 5. 都不是就是 WA
```

**为什么 TLE/MLE 必须在 RE 前面**：超时或内存超限的进程**往往也是非零退出码**（因为被我们杀了）。如果先查 returncode，TLE 会被误判成 RE。所以顺序必须是"先查我们主动判定的超时/超内存，再查退出码"。

### compare_output / _normalize_output：输出比对

```python
def _normalize_output(s):
    s = s.replace("\r\n", "\n").replace("\r", "\n")   # 统一 Windows 换行
    lines = s.split("\n")
    while lines and lines[-1].strip() == "":
        lines.pop()                  # 去掉末尾多余空行
    lines = [ln.rstrip() for ln in lines]   # 去掉每行行尾空格
    return "\n".join(lines)

def compare_output(actual, expected):
    return _normalize_output(actual) == _normalize_output(expected)
```

**宽容的边界**：只宽容"行尾空格"和"末尾空行"。用户输出 `"3\n"`、`"3  \n"`、`"3"` 都算对。但中间内容必须严格一致。这是主流 OJ 的标准做法。

### 编译错误脱敏 _summarize_compile_error

g++ 的原始报错长这样：
```
D:\Lai Siyu\...\data\tmp\ed580a6b...\main.cpp:1:1: error: 'import' does not name a type
In file included from D:\mingw64\...\iostream:38:
...
```

里面全是**服务器本地的绝对路径**，直接展示给用户会泄露目录结构。脱敏后只保留：
```
main.cpp:1:1: error: 'import' does not name a type
```
规则：丢掉所有 `In file included from` 行（头文件展开链）、丢掉含盘符路径的行，只留"main.cpp 里的行号级错误"。

## 3.6 judge/queue.py —— 评测队列

### 为什么要有队列

提交请求进来时，路由只做轻量校验就返回"评测中（pending）"，真正耗时的评测交给后台 worker 慢慢做。中间靠一个 `asyncio.Queue` 衔接。

```python
_queue = asyncio.Queue()          # 全局任务队列

def enqueue(submission_id):       # 路由调用：入队
    _queue.put_nowait(submission_id)

async def _worker():              # 后台协程：循环消费
    while True:
        submission_id = await _queue.get()     # 没任务就挂起等
        submission = storage.get_submission(submission_id)
        if submission is None or submission["status"] != "pending":
            continue               # 状态不对就跳过
        # 评测前检查：题目已被删 → 丢弃
        if storage.get_problem(submission["problem_id"]) is None:
            continue
        result = await judge_submission(submission)
        # 评测后复查：评测期间题目被删 → 丢弃结果（不写回）
        if storage.get_problem(result["problem_id"]) is None:
            continue
        _update_user_stats(result)          # 更新用户提交数/通过数
        storage.save_submission(result)     # 落盘
```

**三个设计点**：

1. **串行单 worker**：一次只评一个。课程要求"单用户任务"，串行最简单、行为最可控（不会互相抢 CPU 导致计时不准）。
2. **双重删题检查**（评测前 + 评测后）：防止"评测进行中管理员删了题"，结果又写回磁盘造成残留数据。
3. **启动恢复 requeue_stale_pending**：服务重启后，磁盘上还停在 pending 的提交会被重新入队——重启不丢评测。

### _update_user_stats：用户统计（一题只计一次）

```python
user["submit_count"] += 1                    # 每次提交 +1
if pid not in user["attempted_problems"]:    # 提交过的题（按题去重）
    user["attempted_problems"].append(pid)
if _is_full_score(submission):               # 满分才算"通过"
    if pid not in user["resolved_problems"]: # 首次通过才 +1
        user["resolved_problems"].append(pid)
        user["resolve_count"] += 1
```

通过率 = `resolve_count / len(attempted_problems)`（通过题数 ÷ 提交过的题数）。这个口径改过三次：提交次数做分母（量纲乱）→ 题库总题数（没碰过的题拉低率）→ **提交过的题数**（最终定稿）。

## 3.7 routers/ —— 六组 API 路由

每个文件就是一组 HTTP 接口。**路由的职责是"门面"**：校验参数 → 查权限 → 调 storage/judge 干活 → 返回统一格式的 JSON。

统一响应格式（schemas.py）：
```python
def ok(data=None, msg="success"):   return {"code": 200, "msg": msg, "data": data}
def err(status_code, msg):          return {"code": status_code, "msg": msg, ...}
```

**异常处理顺序**（main.py 里统一处理）：401 未登录 → 403 无权限/banned → 400 参数错 → 429 频率超限 → 409 冲突 → 404 不存在 → 500 兜底。每个接口不需要自己写 try/except，全靠这一套。

各组路由的要点：

- **problems.py**：增删改查题目。`delete_problem` 是**级联删除**——删题目文件 + 删该题全部提交 + 删相关审计日志 + 回退用户统计（这个"级联"是验收按老师反馈修过的）。
- **submissions.py**：提交入口（校验题目/语言存在 + 频率限制 → 入队）、列表（筛选+分页）、详情、日志、rejudge（管理员，状态打回 pending 重新入队）。
- **users.py**：注册（用户名 3-40、密码 ≥6）、登录（bcrypt 校验 + 写 session）、登出（版本号 +1 让旧 cookie 失效）、角色变更（内置 admin 账号锁定不可改）。
- **languages.py**：语言是**动态注册**的——`languages.json` 里每门语言配 `file_ext / compile_cmd / run_cmd / time_limit / memory_limit`，比如 C++ 是 `g++ {src} -o {exe}` 然后 `{exe}`。这意味着系统支持任意语言，不只是 C++/Python。
- **logs.py**：访问审计——每次有人查提交日志，记录"谁、查了谁的、什么时间、成功与否"，只有管理员能看审计列表。
- **ai.py**：模型配置（密钥不明文回显，只回 `api_key_configured: true`）、命题任务的创建/列表/详情/取消。

## 3.8 ai/llm.py —— 调用大模型

### call_llm 的核心：重试 + 硬超时

```python
async def call_llm(messages, temperature=0.7, ...):
    url = provider_url + "/chat/completions"      # OpenAI 兼容接口
    payload = {"model": model, "messages": messages,
               "temperature": temperature, "max_tokens": max_tokens}

    for attempt in range(4 + 1):                  # 最多 5 次尝试
        try:
            data = await asyncio.wait_for(_post(), timeout=600)
            break
        except asyncio.TimeoutError: raise        # 超时不重试
        except httpx.HTTPStatusError: raise       # 4xx/5xx 不重试
        except (httpx.HTTPError, OSError) as e:   # 连接类错误重试
            last_err = e
            if attempt >= 4: raise
            await asyncio.sleep(2 * (attempt + 1))   # 间隔递增
```

**分两类错误**（这是理解点）：
- **连接类错误**（SSL 握手失败、连接重置、断网）——请求根本没送到模型，不产生费用，**值得重试**；
- **业务错误**（401 key 错了、500 模型挂了）——重试也没用，直接抛。
- **超时**——可能是模型真的在慢慢生成，也可能挂了；为控制总时长不重试。

**为什么有双层超时**：httpx 自己的超时在"流式响应"下会被重置失效（每收到一个数据块，读超时就重新计时），模型如果一直吐字就永远不超时。所以外面再套一层 `asyncio.wait_for`，不管底层什么行为，到 600 秒一定强制返回。

### parse_problem_json：模型输出不一定听话

模型可能会输出 `` ```json ... ``` `` 包裹的代码块、可能被 max_tokens 截断成半个 JSON。解析要兜底：

1. 先剥离 ``` 标记直接 json.loads；
2. 失败 → 找第一个 `{` 到最后一个 `}` 再试；
3. 还是失败 → `_repair_truncated_json` 三种修复策略：
   - **策略 1**：在最后一个"深度为 1 的逗号"处截断补 `}`（保留最多完整字段，最保守）；
   - **策略 2**：扫描未闭合的括号，从后往前补 `]`/`}`；
   - **策略 3**：逐字符从末尾回退，在每个 `:`/`,` 处截断试解析（最激进）。

### calc_cost：费用计算

```python
cost = input_tokens / price_unit * input_price
     + output_tokens / price_unit * output_price
```

输入、输出分开计价（模型定价输入便宜输出贵），`price_unit` 是计价单位（通常每百万 token）。实测：简单题 ¥0.007，复杂题带大测试点 ¥0.053。

## 3.9 ai/engine.py —— AI 命题流水线（六阶段）

```python
async def run_problem_task(task_id):       # 后台协程跑整个流水线
    task["status"] = "running"             # 每个阶段都更新 progress 字段
    # ① 生成题目主体（不含 testcases，控制单次输出长度）
    content, usage = await llm.call_llm([...])
    # ② 解析 JSON（parse_problem_json 各种兜底）
    problem = llm.parse_problem_json(content)
    # ③ 按 testcase_plan 分批生成小测试点
    testcases = await _generate_testcases(task, problem, min_cases=small_n)
    # ④ 标程重算验证（在 _generate_testcases 内部完成）
    # ⑤ 生成器产出大测试点（可选）
    if large_n > 0:
        large = await _generate_large_testcases(task, problem, large_cases=large_n)
    # ⑥ 校验补全 → completed
```

**每个阶段的真实动机**（都对应一次翻车）：

| 阶段 | 解决什么问题 |
|---|---|
| 分阶段生成 | 整题+测试点一次输出超 token 被截断 → JSON 解析失败 |
| testcase_plan | 测试点小/大构成不能写死，由模型按题目性质规划（考复杂度的题大测试点多） |
| 标程重算 | 模型手算测试点答案会错（90 写成 80）→ 用户正确代码被误判 WA |
| 坏数据丢弃 | 模型生成违反输入保证的数据（声明 8 行实际 7 行）→ 正常解法全 RE |
| 生成器脚本 | 百万级测试点模型直接输出不了（token 上限）→ 模型写脚本、本地执行生成 |
| 空内容重试 | 网络抖动返回空响应 → 生成器静默失败只剩小测试点 |

**标程验证的细节**（_verify_testcases_with_solver）：
1. 让模型写一段 Python 标程；
2. **先用题面样例自检标程**——标程连样例都对不上，说明标程本身是错的，不敢用它；
3. 自检通过后，用标程**重算每个测试点的 output，覆盖模型手算的答案**；
4. 标程都跑崩的测试点 = 输入违反题目保证的坏数据 → **丢弃**并记 `verify_note`。

**大测试点生成**（_generate_large_testcases）：
1. 让模型写「数据生成器 + 标程」的 Python 脚本（不是直接输出数据，是输出"造数据的程序"）；
2. 脚本里 `generate_input(seed)` 按种子生成输入（分档递进：中间 seed 用 30%~60% 规模、最后一个逼近上限），`solve(data)` 是正确解法；
3. 本地执行脚本，按 `===CASE n INPUT===` 标记切分每组数据；
4. 实测产出过 728 万字符的压轴测试点。

**中断机制**（cancel_task）：
```python
_RUNNING_TASKS = {}            # task_id → asyncio.Task 协程句柄
def cancel_task(task_id):
    t = _RUNNING_TASKS.get(task_id)
    t.cancel()                 # 触发 CancelledError，打断正在 await 的 HTTP 请求
```
只改状态字段是"假中断"（这轮模型调用还在跑），`task.cancel()` 才真正打断。`run_problem_task` 里 `except asyncio.CancelledError → 标记 cancelled`。

**僵尸任务恢复**（recover_stale_tasks）：命题协程是内存态的，服务重启后不会自动继续。启动时把磁盘上遗留 pending/running 的任务标记为 failed（提示重新提交），避免永久卡住。

## 3.10 frontend/app.py —— Streamlit 前端（1991 行）

前端是"瘦客户端"：只负责展示和调 API，逻辑都在后端。两个技术点：

### 轮询：st.fragment(run_every="2s")

评测/AI 任务在跑时，前端要不断问后端"好了没"。最初用 `st.rerun()` 无限循环刷新，触发 Streamlit 已知 bug（元素变少时旧 DOM 残留，页面叠层）。

改用 `st.fragment(run_every="2s")`：只让那一小块区域每 2 秒重跑，不动整页；任务结束才 rerun 一次渲染结果。**再进一步做了快照对比**：每 2 秒查接口，但只有状态/进度/Token 变化才刷新页面——数据不变页面静止、不闪烁。

### 大测试点折叠

生成器产出百万字符的测试点，塞进表单 textarea 会卡死浏览器。方案：
- 默认折叠：大测试点（>2000 字符）只显示摘要"输入 167 万字符（已保留）"；
- "展开编辑"开关放在**表单外**（Streamlit 表单内控件不即时生效）；
- 没展开就提交 → 直接拿后端 prefill 的完整数据组装请求，数据不丢。

### 会话持久化

cookie 存登录态；刷新页面用 localStorage 恢复 session（不然每次刷新都回登录页——这是验收前修过的一个 bug）。

---

# 第四部分：把代码串起来的三个"完整故事"

## 故事一：一次代码提交的一生

```
1. 用户在提交页选题目、选语言、粘代码、点提交
2. 前端 POST /api/submissions/（code, problem_id, language）
3. 路由：登录校验(401) → banned 检查(403) → 题目存在(404) → 语言存在(400)
        → 频率限制 check_rate_limit(429) → 生成 id → 状态 pending → 落盘
4. enqueue(id) 放进 asyncio.Queue
5. 后台 worker 拿到 id → 读提交 → judge_submission：
   a. 建临时目录、写 main.cpp
   b. compile_source（30 秒独立时限）→ 失败则全 CE、0 分
   c. 逐个测试点：run_source（3 秒时限 + 128MB 内存限制）
      - runner.run_command：起子进程 + 内存监控协程 + wait_for 卡时间
      - 超时杀进程→TLE；内存超限杀进程→MLE；非零退出→RE
      - compare_output 归一化比对 → AC / WA
   d. score = AC 数 × 10
6. 删题双重检查 → _update_user_stats（提交数+1，通过题数按题去重+1）→ 落盘
7. 前端提交详情页 st.fragment 每 2 秒轮询，status 变 success 后渲染测试点明细表
```

## 故事二：一次 AI 命题的一生

```
1. 用户输入需求"考察贪心的任务安排题"
2. POST /api/ai/problem-tasks/ → 任务 pending → start_task 后台协程跑起来
3. ① LLM 生成题目主体 + testcase_plan（如 small 6 + large 4）
4. ② parse_problem_json 解析（各种截断兜底）
5. ③ 分批生成 6 个小测试点（每批 4 个，网络抖动重试）
6. ④ 模型写标程 → 跑样例自检 → 重算每个测试点 output 覆盖手算答案
     → 标程跑崩的坏数据丢弃
7. ⑤ 模型写生成器脚本 → 本地执行产出 4 个大测试点（分档逼近上限）
8. ⑥ 校验补全 → completed，任务详情页展示结果表单
9. 用户点"导入题库" → POST /api/problems/ → 成为正式题目，可被提交评测
```

## 故事三：删除一道题会连锁发生什么

```
1. DELETE /api/problems/{id}（仅管理员）
2. 删题目 JSON 文件
3. 删该题全部提交文件（delete_submissions_of_problem）
4. 删审计日志里该题所有记录（remove_audit_logs_of_problem）
5. 回退用户统计：submit_count 减去该题提交数；
   attempted_problems / resolved_problems 集合移除该题
6. worker 侧双保险：评测前/后查题目，不存在就丢弃任务不写回
```

---

# 第五部分：助教问题的"万能回答框架"

被问到任何功能，按这个框架答（顺序即思路）：

1. **它在哪**："在 `app/judge/runner.py` 的 `run_command` 函数"
2. **它在数据流里的位置**："评测时每个测试点都会调它，它的输入是一条命令+输入数据，输出是运行结果（退出码/输出/耗时/峰值内存）"
3. **核心机制一句话**："用 `asyncio.wait_for` 卡时间，用 psutil 轮询卡内存"
4. **一个细节**（体现你真的懂）："判定顺序必须先查超时再查退出码，因为被杀的进程退出码也是非零的"
5. **对应哪个需求/修过什么坑**："这是 Step 2 的资源限制要求；内存监控当初用跑完再查会读不到，改成边跑边采样"

这套框架任何问题都适用。核心是**顺着数据流想**，而不是背知识点。

# OJ 系统函数全览 —— 每个函数是干什么的

> 用法：助教问"XX 在哪实现的"，先在"总览地图"里定位文件，再在该文件的函数表里找到对应函数。
> 行号为 2026-09-09 验收当日代码，若有改动以实际为准。

---

# 总览地图（先记这一页）

```
前端 frontend/app.py（1991 行）
  └─ api_call()：所有页面统一从这里发 HTTP 请求到后端

后端入口 app/main.py（142 行）
  ├─ on_startup()：启动时做的事（建目录/初始管理员/恢复中断任务）
  ├─ _seed_languages()：注册内置的 python 和 cpp 两门语言
  └─ system_reset()：一键重置系统

路由层 app/routers/（六个文件 = 六组 HTTP 接口）
  ├─ problems.py    题目增删改查 + 日志可见性开关
  ├─ submissions.py 提交、提交列表、提交详情、日志查询、重新评测
  ├─ users.py       注册/登录/登出/用户列表/角色管理
  ├─ languages.py   语言列表、动态注册新语言
  ├─ logs.py        访问审计日志查询
  └─ ai.py          模型配置、命题任务创建/查询/取消

中间件 app/deps.py（54 行）
  ├─ get_current_user()：每次请求前解析"你是谁"（session + 会话版本号）
  ├─ is_admin()：是不是管理员
  └─ check_rate_limit()：提交频率限制（单人单题 1 分钟 3 次）

评测引擎 app/judge/（三件套）
  ├─ queue.py  评测队列（提交进来排队，后台 worker 一个个评）
  ├─ engine.py 评测流程（编译→逐测试点运行→比对→判定→算分）
  └─ runner.py 进程执行器（拉子进程、卡时间、盯内存、杀进程）

AI 命题 app/ai/
  ├─ llm.py    调用大模型（重试/超时/费用/解析兜底）
  └─ engine.py 命题流水线（六阶段 + 标程验证 + 生成器 + 中断）

存储 app/storage.py
  └─ 所有 JSON 文件读写的唯一出入口（含读缓存）
```

---

# 一、app/main.py（后端入口）

| 函数 | 作用 |
|---|---|
| `app = FastAPI(...)` | 创建 FastAPI 应用实例，所有路由都挂在这个 app 上 |
| `_validation_exception_handler()` | 全局异常处理器：Pydantic 字段校验失败时，把英文错误转成中文（"字段「xxx」缺失"）统一返回 400 |
| `SessionMiddleware` | 会话中间件：让每个请求都带上 session（存登录态），密钥在 config.py |
| `CORSMiddleware` | 跨域中间件：允许前端（不同端口的 Streamlit）调用后端接口 |
| `on_startup()` | **启动时执行**：① 建数据目录；② 确保初始管理员 admin 存在；③ 注册内置语言；④ 启动评测 worker；⑤ 旧用户补齐 attempted_problems 字段（通过率新口径的迁移）；⑥ 磁盘上遗留的 pending 提交重新入队（重启不丢评测）；⑦ 遗留的 AI 命题 pending/running 任务标记失败（协程是内存态的，重启不恢复） |
| `on_shutdown()` | 关闭时停掉评测 worker 协程 |
| `_seed_languages()` | 注册内置语言：python（直接解释运行）、cpp（`g++ {src} -o {exe}` 编译后运行）；顺带修复早期写入的 `-std=c++17`（和本机 MinGW 冲突导致 CE） |
| `root()` | `GET /` 健康检查，返回服务运行状态 |
| `system_reset()` | `POST /api/reset/`（仅管理员）：清空全部数据、清 session、清频率限制记录、重建初始管理员 |

# 二、app/routers/problems.py（题目管理）

| 函数 | 作用 |
|---|---|
| `_validate_problem(p)` | 校验题目数据的合法性：必填字段非空、samples/testcases 是列表且非空、每个样例/测试点含 input+output。返回错误文案或 None |
| `list_problems()` | `GET /api/problems/`：返回全部题目（只含 id 和 title 的简表） |
| `create_problem()` | `POST /api/problems/`：校验 → 查重（id 冲突返回 409）→ 补默认字段 → 落盘 |
| `get_problem_info()` | `GET /api/problems/{id}`：返回完整题目；可选字段补默认值（str→""、list→[]） |
| `update_problem()` | `PUT /api/problems/{id}`：整体更新；要求 body 里的 id 与路径一致；**保留 public_cases 配置**（防止编辑题目时丢失可见性设置） |
| `delete_problem()` | `DELETE /api/problems/{id}`（仅管理员）：**级联删除四步**——① 回退用户统计（该题的提交数、通过题数、提交过题数）；② 删题目文件；③ 删该题全部提交；④ 删审计日志中该题记录 |
| `set_log_visibility()` | `PUT /api/problems/{id}/log_visibility`（仅管理员）：设置 `public_cases` 开关——决定普通用户能否看该题提交的测试点日志 |

# 三、app/routers/submissions.py（提交评测）

| 函数 | 作用 |
|---|---|
| `create_submission()` | `POST /api/submissions/`：登录校验 → 题目存在(404) → 语言存在(404) → 频率限制(429，管理员免限) → 生成 id、状态 pending、落盘 → **入队**，立即返回"评测中" |
| `list_submissions()` | `GET /api/submissions/`：支持按用户/题目/状态筛选 + 分页；非管理员只能看自己；返回摘要（不包含测试点明细） |
| `get_submission_info()` | `GET /api/submissions/{id}`：仅本人或管理员；pending 时只返回基本信息，评完后返回得分/编译信息/运行信息 |
| `get_submission_log()` | `GET /api/submissions/{id}/log`：返回每个测试点的结果/时间/内存明细。权限三级：本人 / 管理员 / `public_cases=true` 时所有登录用户。**每次访问都写审计日志**（成功 200、越权 403 都记） |
| `rejudge()` | `PUT /api/submissions/{id}/rejudge`（仅管理员）：把状态重置回 pending、清空旧结果、重新入队评测；正在评测中的返回 409 |

# 四、app/routers/users.py（用户管理）

| 函数 | 作用 |
|---|---|
| `_hash_password()` / `_verify_password()` | 用 **bcrypt** 加密/校验密码（数据库里只存哈希，不存明文） |
| `_user_public()` | 把用户数据转成对外返回的格式（**不含密码哈希**），含 submit_count / resolve_count / attempted_count |
| `ensure_initial_admin()` | 启动时调用：如果不存在用户名 admin，就创建初始管理员 admin / admintestpassword |
| `login()` | `POST /api/auth/login`：按用户名找用户 → bcrypt 校验密码 → banned 检查 → 把 user_id 和**会话版本号**写进 session |
| `logout()` | `POST /api/auth/logout`：**把用户数据的 session_version +1**（所有旧 cookie 立即失效）→ 清 session |
| `register()` | `POST /api/users/`：用户名 3-40 位、密码 ≥6 位、用户名查重 → 创建普通用户 |
| `create_admin()` | `POST /api/users/admin`（仅管理员）：同注册但角色为 admin |
| `list_users()` | `GET /api/users/`（仅管理员）：用户列表 + 分页，含提交数/通过数/提交过题数统计 |
| `get_user_info()` | `GET /api/users/{id}`：仅本人或管理员 |
| `update_role()` | `PUT /api/users/{id}/role`（仅管理员）：改角色 user/admin/banned；**内置 admin 账号锁定不可改**（防止唯一管理员被降权导致系统失控） |

# 五、app/routers/languages.py + logs.py（语言 + 审计）

| 函数 | 作用 |
|---|---|
| `list_languages()` | `GET /api/languages/`：返回语言名列表（不要求登录） |
| `register_language()` | `POST /api/languages/`：动态注册新语言——提供 file_ext（源文件后缀）、compile_cmd（编译命令，解释型可空）、run_cmd（运行命令）、默认时限/内存。**这就是"语言可扩展"的实现** |
| `list_access_logs()` | `GET /api/logs/access/`（仅管理员）：查看访问审计日志——谁在什么时候查了谁的提交日志、成功与否 |

# 六、app/deps.py（认证与限流，被几乎所有接口调用）

| 函数 | 作用 |
|---|---|
| `get_current_user()` | **每个接口第一行都会调它**：从 session 读 user_id → 查用户 → **校验会话版本号 sv**（对不上 = 已登出/会话失效 → 返回 None → 接口回 401） |
| `is_admin()` | 判断用户 role 是否为 admin |
| `check_rate_limit()` | 频率限制：按 `(user_id, problem_id)` 计数，同一用户同一题 1 分钟内第 4 次提交返回 False → 接口回 429；换题不受影响 |
| `reset_rate_limits()` | 清空频率限制记录（系统重置时用） |

# 七、app/judge/runner.py（进程执行器 —— 时间/内存限制在这里）

| 函数 | 作用 |
|---|---|
| `RunResult`（dataclass） | 一次运行的完整结果：退出码、标准输出、标准错误、耗时、峰值内存、是否超时、是否内存超限 |
| `_monitor_memory()` | **内存监控协程**：每 50ms 用 psutil 采样进程内存（主进程 + 递归子进程），记录峰值；超过限制立即标记 MLE 并杀进程 |
| `_kill()` | 强制杀进程：先杀所有子进程再杀主进程（防止子进程残留） |
| `run_command()` | **核心执行函数**：① `create_subprocess_exec` 拉起子进程（三根管道接上）；② 同时启动内存监控协程；③ `asyncio.wait_for(..., timeout=time_limit)` 卡时间——超时抛异常标记 TLE；④ 超时杀进程；⑤ 返回 RunResult |

# 八、app/judge/engine.py（评测流程）

| 函数 | 作用 |
|---|---|
| `resolve_placeholders()` | 把语言命令模板里的 `{src}`/`{exe}` 替换成带引号的实际路径（防空格路径出问题） |
| `_parse_command()` | 用 shlex 把命令字符串拆成参数列表（正确处理引号） |
| `_prepare_temp_dir()` | 为每次评测建独立临时目录（uuid 命名，互不干扰） |
| `_normalize_output()` | 输出归一化：统一换行符、去行尾空格、去末尾空行 |
| `compare_output()` | 归一化后比对实际输出和期望输出 |
| `compile_source()` | 执行编译命令（无 compile_cmd 的解释型语言直接跳过） |
| `run_source()` | 执行运行命令，注入测试点输入 |
| `judge_submission()` | **评测主流程**：取题目/语言 → 资源限制三级兜底（题目 > 语言 > 默认）→ 建临时目录写源码 → 编译（独立 30 秒时限）→ 失败全 CE → 逐个测试点运行+判定 → 算分（AC 数×10）→ finally 清理临时目录 |
| `_summarize_compile_error()` | 编译错误**脱敏**：g++ 原始输出含服务器绝对路径，只保留 `main.cpp:行号: error:` 级别的错误行 |
| `_classify()` | 判定单个测试点：**顺序固定** 超时→TLE、内存超限→MLE、非零退出→RE、执行器异常→UNK、输出一致→AC、否则→WA（顺序不能换，被杀进程退出码也是非零） |

# 九、app/judge/queue.py（评测队列）

| 函数 | 作用 |
|---|---|
| `enqueue()` | 把提交 id 放进 asyncio.Queue（路由提交后调用） |
| `requeue_stale_pending()` | 启动恢复：扫描磁盘上遗留的 pending 提交重新入队 |
| `_worker()` | **后台 worker 协程**：无限循环从队列取任务 → 读提交 → 评测前检查题目存在 → `judge_submission` 评测 → 评测后复查题目存在 → 更新用户统计 → 落盘。**串行单 worker**（一次评一个，满足"单用户任务"） |
| `_is_full_score()` | 判断提交是否满分（score ≥ 测试点数 × 10 且分数 > 0） |
| `_rebuild_resolved()` | 从历史提交重建用户的"已通过题目"集合（旧数据迁移用） |
| `_rebuild_attempted()` | 从历史提交重建用户的"提交过的题目"集合（旧数据迁移用） |
| `_update_user_stats()` | 更新用户统计：submit_count +1；attempted_problems 按题去重；满分且首次通过才 resolve_count +1 |
| `start_worker()` / `stop_worker()` | 启动/停止后台评测协程 |

# 十、app/ai/llm.py（大模型调用）

| 函数 | 作用 |
|---|---|
| `get_model_config()` | 读模型配置（**不含明文密钥**，只回 api_key_configured 布尔值） |
| `get_model_config_raw()` | 读完整配置含密钥（仅内部用） |
| `save_model_config()` | 保存模型配置 |
| `call_llm()` | **调用大模型核心**：拼 chat/completions 请求 → 外层 `asyncio.wait_for` 硬超时（600s）→ **连接类错误自动重试 4 次**（SSL 失败/连接重置），业务错误（401/500）和超时不重试 → 返回 (内容, 用量) |
| `calc_cost()` | 费用计算：输入/输出 token 分别乘单价再除以计价单位 |
| `parse_problem_json()` | 解析模型返回的题目 JSON：剥离 ``` 代码块 → 直接解析 → 截取 { } → 三种截断修复 |
| `_repair_truncated_json()` | 截断 JSON 修复三策略：① 顶层逗号处截断补 }（最保守）；② 补未闭合括号；③ 逐字符回退丢弃末尾字段 |

# 十一、app/ai/engine.py（AI 命题流水线）

| 函数 | 作用 |
|---|---|
| `_get_task()` / `_save_task()` / `_new_task()` | 任务 JSON 的读/写/创建（创建含 usage 计费字段初始化） |
| `list_task_ids()` | 列出所有命题任务 id |
| `recover_stale_tasks()` | 启动恢复：把遗留的 pending/running 任务标记 failed（服务重启后协程消失，不标记会永久卡住） |
| `_accumulate_usage()` | 把每次 LLM 调用的 token 用量累加到任务上 |
| `_build_requirement_prompt()` | 把用户需求 + 参考题目拼成发给模型的 prompt |
| `_check_cancelled()` | 每个阶段间检查任务是否被中断 |
| `run_problem_task()` | **命题流水线主函数（后台协程跑）**：阶段① 生成题目主体+testcase_plan → ② 解析 JSON → ③ 分批生成小测试点 → ④ 标程重算验证 → ⑤ 生成器产出大测试点 → ⑥ 校验补全 → completed。CancelledError → cancelled；其他异常 → failed |
| `_generate_testcases()` | 分批生成小测试点：每批 4 个凑够规划数量；单批网络抖动不整体失败 |
| `_verify_testcases_with_solver()` | **标程验证**：让模型写标程 → 跑题面样例自检（不过就不敢用）→ 用标程重算每个测试点 output 覆盖手算答案 → 标程跑崩的测试点 = 坏数据丢弃 |
| `_extract_code()` | 从模型返回文本中提取代码（剥离 markdown 代码块） |
| `_generate_large_testcases()` | **生成器大测试点**：让模型写"数据生成器+标程"脚本 → 本地执行 → 按 `===CASE n INPUT===` 标记切分 → 产出逼近 constraints 上限的大数据 |
| `_normalize_problem()` | 校验必填字段、补默认值 |
| `start_task()` | 用 `asyncio.create_task` 在后台启动流水线，登记到全局协程注册表 |
| `cancel_task()` | **真正的中断**：从注册表取协程句柄 `task.cancel()` → 触发 CancelledError 立即打断正在 await 的 HTTP 请求 |

# 十二、app/ai/routers/ai.py（AI 命题接口）

| 函数 | 作用 |
|---|---|
| `set_model_config()` | `PUT /api/ai/model-config`：改模型配置；**api_key 留空 = 不修改**（防止改单价时误清密钥） |
| `get_model_config()` | `GET /api/ai/model-config`：查配置（不含密钥） |
| `create_problem_task()` | `POST /api/ai/problem-tasks/`：需求非空校验 → 模型已配置校验 → 建任务 → 后台启动流水线 |
| `list_problem_tasks()` | 任务列表：非管理员只看自己的；支持状态筛选 + 分页 |
| `get_problem_task()` | 任务详情：状态/进度/用量/结果/错误信息 |
| `cancel_problem_task()` | 取消任务：先 `engine.cancel_task()` 真正打断协程，再置状态（已完成的任务取消失败 409） |

# 十三、app/storage.py（存储层）

| 函数 | 作用 |
|---|---|
| `_read_json()` | 读 JSON 文件：文件不存在返回默认值；**mtime+size 缓存**（文件没变不重新读盘，列表接口 1.6s→0.04s） |
| `_write_json()` | 写 JSON 文件：先写 .tmp 再 `os.replace` **原子替换**（防半写损坏），写后失效缓存 |
| `new_id()` | 生成短随机 id（提交/任务/用户用） |
| `now_str()` | 当前时间字符串 |
| `list_problem_ids()` / `get_problem()` / `save_problem()` / `delete_problem()` | 题目文件的列/读/写/删 |
| `delete_submissions_of_problem()` | 删该题全部提交文件（级联删除用） |
| `remove_audit_logs_of_problem()` | 删审计日志中该题记录（级联删除用） |
| `remove_problem_from_users_resolved()` | 回退用户统计：submit_count 减该题提交数、resolved/attempted 集合移除该题 |
| `get_languages()` / `save_languages()` | 语言配置读/写 |
| `get_users()` / `save_users()` | 用户数据读/写 |
| `get_submission()` / `save_submission()` / `list_submission_ids()` | 提交读/写/列 |
| `get_audit_logs()` / `append_audit_log()` | 审计日志读/追加 |
| `reset_all()` | 清空全部数据（系统重置用） |

# 十四、frontend/app.py（前端，按页面组织）

| 函数 | 作用 |
|---|---|
| `api_call()` | **所有前端请求的统一出口**：带 cookie、统一错误处理，返回 (状态码, 数据, 错误信息) |
| `is_logged_in()` / `current_user()` | 登录态判断 / 获取当前用户信息 |
| `_restore_session()` / `_sync_localstorage()` | 刷新页面后用 localStorage 恢复会话（不然每次刷新都掉登录） |
| `logout()` | 前端登出 |
| `inject_css()` | 注入自定义样式（标签配色、卡片等） |
| `render_topbar()` | 顶部导航栏（题目/评测提交/用户/用户管理/AI 命题/退出） |
| `render_login()` | 登录/注册页 |
| `render_user_info()` | 用户信息页：统计卡片（提交次数/通过题数/**通过率**） |
| `_problem_payload()` | 把表单字段组装成题目请求体；JSON 解析失败返回 None |
| `_render_big_tc_toggle()` | "展开编辑大测试点"开关（**放在表单外**，因为 Streamlit 表单内控件不即时生效） |
| `_problem_form()` | 题目表单（新增/编辑/AI 导入共用）：**大测试点默认折叠为摘要**防卡死，展开才显示全文 |
| `_paginated_list()` | 通用分页组件 |
| `render_problem_list()` | 题目列表页 |
| `render_problem_create()` / `render_problem_edit()` | 新增/编辑题目页 |
| `render_problem_detail()` | 题目详情页（题面+样例，样例输入输出分行展示） |
| `render_submission()` | 提交代码页 |
| `render_submission_list()` | 提交记录列表页 |
| `render_submission_detail()` | 提交详情页：统计卡片 + 测试点明细表；pending 时用 `st.fragment(run_every="2s")` 轮询 |
| `render_user_admin()` | 用户管理页（列表/改角色/访问审计） |
| `render_ai()` / `_ai_config_dialog()` / `render_ai_task_list()` / `render_ai_problem()` / `render_ai_task_detail()` | AI 命题五个子页面：主入口、模型配置弹窗、历史任务列表、出题表单、任务详情（快照对比轮询） |
| `_clear_view_state()` | 清空所有子视图状态（切换菜单时防页面叠层） |
| `main()` | 前端主入口：按登录态和菜单状态路由到各页面 |

---

# 高频问答（对应"功能↔函数"）

| 助教问 | 答（函数 + 一句话） |
|---|---|
| 时间限制怎么检查的？ | `runner.py` 的 `run_command`：`asyncio.wait_for` 卡时间，超时 `_kill` 杀进程 |
| 内存限制怎么检查的？ | `runner.py` 的 `_monitor_memory`：psutil 每 50ms 轮询峰值，超限杀进程 |
| 为什么 TLE 要在 RE 前面判定？ | `engine.py` 的 `_classify`：被杀进程退出码也非零，顺序反了会把 TLE 误判成 RE |
| 提交后发生了什么？ | `submissions.py` 的 `create_submission` 入队 → `queue.py` 的 `_worker` 串行评测 → `engine.py` 的 `judge_submission` 编译+逐点运行 |
| 频率限制在哪？ | `deps.py` 的 `check_rate_limit`：单人单题 1 分钟 3 次 |
| 登录态怎么维护的？ | `deps.py` 的 `get_current_user`：session + 会话版本号，登出+1 使旧 cookie 失效 |
| 通过率怎么算的？ | `queue.py` 的 `_update_user_stats` 维护 attempted/resolved 集合；通过题数 ÷ 提交过题数 |
| AI 怎么保证测试点答案正确？ | `ai/engine.py` 的 `_verify_testcases_with_solver`：标程先过样例自检，再重算 output 覆盖，跑崩即丢弃 |
| 大测试点怎么生成的？ | `ai/engine.py` 的 `_generate_large_testcases`：模型写生成器脚本，本地执行产出逼近上限的数据 |
| AI 任务怎么中断的？ | `ai/engine.py` 的 `cancel_task`：`task.cancel()` 打断正在 await 的 HTTP 请求 |
| 删题会连锁发生什么？ | `problems.py` 的 `delete_problem`：回退统计 + 删题 + 删提交 + 删审计 |
| 为什么读文件要缓存？ | `storage.py` 的 `_read_json`：mtime+size 缓存，列表接口 1.6s→0.04s |
| 为什么写文件要先写 .tmp？ | `storage.py` 的 `_write_json`：`os.replace` 原子替换，防止写到一半损坏 |
